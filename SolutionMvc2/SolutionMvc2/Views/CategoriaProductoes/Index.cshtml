@model IEnumerable<SolutionMvc2.Models.CategoriaProducto>

@{
    ViewBag.Title = "Index";
}

<h2>Categoría de Productos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoría.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoría.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_CategoriaProducto }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_CategoriaProducto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_CategoriaProducto })
        </td>
    </tr>
}

</table>
