@model IEnumerable<SolutionMvc2.Models.Imagen>

@{
    ViewBag.Title = "Index";
}

<h2>Imagen</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Exportada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Exportada)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Imagen }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Imagen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Imagen })
        </td>
    </tr>
}

</table>
